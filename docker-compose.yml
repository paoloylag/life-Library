services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: library_attendance
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: ["library_data:/var/lib/postgresql/data"]
volumes:
  library_data:

